MONGO_URI=mongodb://127.0.0.1:27017/exam-proctoring
JWT_SECRET=your_super_secret_jwt_key_change_in_production
PORT=5000
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development
ALLOW_SEED=true
AUTO_APPROVE_STUDENTS=true

# Optional: your email becomes admin on next login (dev only)
# BOOTSTRAP_ADMIN_EMAIL=admin@demo.proctorai.com

# Optional — OpenAI for AI question generation
OPENAI_API_KEY=your_openai_api_key

# Optional — SMTP for emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_smtp_app_password
SMTP_FROM=your_email@gmail.com
SMTP_SECURE=false